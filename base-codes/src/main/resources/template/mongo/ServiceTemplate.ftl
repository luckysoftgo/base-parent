package ${baseFilePackage}.service.${entityPackage};

import ${baseFilePackage}.entity.${entityPackage}.${className};
import com.application.base.mongo.spring.api.MongoBaseService;

/**
 * service 的接口定义(实际使用中可以自行添加)
 * @author bruce
 */
public interface ${className}Service extends MongoBaseService<${className}> {

}
